- proper cleanup, verification of types being used.

- adapt Task.summarize() to two methods for formatting, single and multiple. Then based on type, use these methods to format.

- move to new sketch or actual project

- implement a group/set/table data structure